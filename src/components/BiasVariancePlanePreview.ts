/** V6 — Bias-Variance Plane Preview. Placeholder until Step 7. */
class BiasVariancePlanePreview extends HTMLElement {
  connectedCallback() {
    this.innerHTML = `<div class="panel-placeholder" style="height:360px;display:flex;align-items:center;justify-content:center;background:var(--rl-surface);border:1px dashed var(--rl-border);border-radius:6px;color:var(--rl-ink-muted);font-family:var(--rl-mono)">V6 · BiasVariancePlanePreview — coming in Step 7</div>`;
  }
}
customElements.define("bias-variance-plane-preview", BiasVariancePlanePreview);
