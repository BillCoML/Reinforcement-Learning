/** V2 — First-Visit vs Every-Visit trajectory walkthrough. Placeholder until Step 4. */
class FirstVsEveryVisitWalkthrough extends HTMLElement {
  connectedCallback() {
    this.innerHTML = `<div class="panel-placeholder" style="height:460px;display:flex;align-items:center;justify-content:center;background:var(--rl-surface);border:1px dashed var(--rl-border);border-radius:6px;color:var(--rl-ink-muted);font-family:var(--rl-mono)">V2 · FirstVsEveryVisitWalkthrough — coming in Step 4</div>`;
  }
}
customElements.define("first-vs-every-visit-walkthrough", FirstVsEveryVisitWalkthrough);
