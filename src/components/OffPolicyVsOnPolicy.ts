/** V5 — Off-Policy vs On-Policy MC comparison. Placeholder until Step 7. */
class OffPolicyVsOnPolicy extends HTMLElement {
  connectedCallback() {
    this.innerHTML = `<div class="panel-placeholder" style="height:460px;display:flex;align-items:center;justify-content:center;background:var(--rl-surface);border:1px dashed var(--rl-border);border-radius:6px;color:var(--rl-ink-muted);font-family:var(--rl-mono)">V5 · OffPolicyVsOnPolicy — coming in Step 7</div>`;
  }
}
customElements.define("off-policy-vs-on-policy", OffPolicyVsOnPolicy);
